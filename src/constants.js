export let GRAVITY_ACC = 9.80665;
export let AIRDENSITY = 1.225; // Dry air density when temp = 15
export let W = 7.2921159 * Math.pow(10, -5); // rad/s angular velocity of earth's rotation (W->E)
export let alpha_coeff = 0.143; // coefficient that varies dependent upon the stability of the atmosphere